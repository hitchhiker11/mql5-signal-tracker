# 🚀 MQL5 Signal Manager

Привет! Это мой проект по автоматизации доверительного управления капиталом управления сигналами с MQL5. Он умеет транслировать необходимую статистку по сигналу в личный кабинет пользователя. Сделал его чтобы упростить жизнь трейдерам и админам, которые работают с сигналами на MQL5.com.

## 🛠 Технологии

### Frontend
- React 
- Material UI
- Axios (для API запросов)
- React Router (для навигации)

### Backend 
- Node.js + Express
- PostgreSQL (хранение данных)
- JWT (авторизация)

## 🔥 Фичи

### Для админов
- Управление пользователями
- Добавление/редактирование сигналов
- Назначение сигналов пользователям
- Статистика по пользователям и сигналам

### Для пользователей
- Просмотр назначенных сигналов
- Статистика по сигналам
- Управление профилем

## 🚀 Как запустить

1. Клонируем репозиторий:
```
git clone https://github.com/your-username/mql5-signal-manager.git
cd mql5-signal-manager
```
2. Устанавливаем зависимости:

```
# Backend
cd mql5-parser
npm install

# Frontend
cd frontend
npm install
```

3. Настраиваем базу данных:
- Создаем PostgreSQL базу
- Копируем `.env.example` в `.env`
- Заполняем данные для подключения

4. Запускаем:

```
# Backend
cd mql5-parser
npm run dev

# Frontend
cd frontend
npm run dev
```

## 💡 Как использовать

1. Регистрируемся как админ
2. Добавляем пользователей
3. Добавляем сигналы через URL с MQL5
4. Назначаем сигналы пользователям
5. Profit! 🎉


## 📝 TODO

- [ ] Добавить тесты
- [ ] Сделать Docker-compose
- [ ] Добавить автоматическое обновление статистики
- [ ] Прикрутить уведомления
- [ ] Сделать мобильную версию

## 📄 Лицензия

делайте что хотите, но автора упомяните 😉

# Meta Trader App

Приложение для анализа торговых сигналов MetaTrader. Парсит данные с сайта MQL5.com и предоставляет удобный интерфейс для анализа торговых сигналов.

## Технологический стек

### Бэкенд
- Node.js
- Express
- MongoDB
- JSDOM для парсинга

### Фронтенд
- React/Vite
- Nginx (для продакшн-деплоя)

## Запуск на сервере с внешним IP

Приложение настроено для работы на сервере с IP-адресом 109.73.192.193. Для запуска на другом сервере отредактируйте переменную `SERVER_IP` в файлах `start.sh` и `stop.sh`.

### Предварительные требования

- Доступ к серверу с правами sudo
- Установленный Docker и Docker Compose
- Открытые порты 80 (HTTP) и 3001 (API)

### Быстрый старт на сервере

1. Клонируйте репозиторий на сервер:
   ```bash
   git clone https://github.com/yourusername/meta_trader_app.git
   cd meta_trader_app
   ```

2. Сделайте скрипты запуска и остановки исполняемыми:
   ```bash
   chmod +x start.sh stop.sh
   ```

3. Запустите приложение:
   ```bash
   sudo ./start.sh
   ```

Скрипт автоматически обновит настройки для работы с внешним IP-адресом сервера, создаст необходимые сети Docker и запустит все контейнеры.

### Доступ к приложению на сервере

- Фронтенд: http://109.73.192.193:80
- API бэкенда: http://109.73.192.193:3001
- MongoDB: доступна только внутри Docker-сети (для безопасности)

### Остановка приложения на сервере

Для остановки контейнеров и создания резервных копий логов:
```bash
./stop.sh
```

## Запуск в локальном Docker-окружении

Если вы хотите запустить приложение локально для разработки или тестирования, выполните следующие шаги:

### Предварительные требования

- Docker
- Docker Compose

### Быстрый старт локально

1. Клонируйте репозиторий:
   ```bash
   git clone https://github.com/yourusername/meta_trader_app.git
   cd meta_trader_app
   ```

2. Отредактируйте переменную `SERVER_IP` в файлах `start.sh` и `stop.sh`, заменив ее на `localhost`:
   ```bash
   # В start.sh и stop.sh замените строку
   SERVER_IP="109.73.192.193"
   # на
   SERVER_IP="localhost"
   ```

3. Запустите приложение:
   ```bash
   chmod +x start.sh
   ./start.sh
   ```

### Доступ к приложению локально

- Фронтенд: http://localhost:80
- API бэкенда: http://localhost:3001
- MongoDB: mongodb://localhost:27017

## Ручная настройка и запуск (без Docker)

Если вы предпочитаете запускать приложение без Docker, следуйте инструкциям ниже.

### Бэкенд

1. Перейдите в директорию бэкенда:
   ```bash
   cd backend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл `.env` из примера и отредактируйте:
   ```bash
   cp .env.example .env
   ```

4. Запустите сервер:
   ```bash
   npm start
   ```

### Фронтенд

1. Перейдите в директорию фронтенда:
   ```bash
   cd frontend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Создайте файл `.env` из примера и отредактируйте:
   ```bash
   cp .env.example .env
   ```

4. Запустите сервер разработки:
   ```bash
   npm run dev
   ```

## Устранение неполадок

### Проблемы с подключением к MQL5.com

Если у вас возникают проблемы с парсингом данных с MQL5.com:

1. Проверьте, доступен ли сайт напрямую из вашего браузера
2. Проверьте сетевые настройки (прокси, файрвол)
3. Очистите кеш парсера: удалите все файлы в директории `/backend/cache/mql5`

### Проблемы с CORS

Если возникают ошибки CORS при обращении к API:

1. Проверьте, что в файле `backend/src/server.js` указаны все необходимые источники в массиве `allowedOrigins`
2. Проверьте правильность настройки Nginx в файле `frontend/nginx.conf`
3. Убедитесь, что запросы идут на правильный IP и порт

### Проблемы с Docker

Если у вас возникают проблемы с Docker:

1. Проверьте логи контейнеров:
   ```bash
   docker-compose logs
   ```

2. Проверьте права доступа к директориям кеша и логов:
   ```bash
   sudo chmod -R 777 ./backend/cache
   sudo chmod -R 777 ./backend/logs
   ```

3. Перезапустите контейнеры с чистой сборкой:
   ```bash
   docker-compose down
   docker-compose build --no-cache
   docker-compose up -d
   ```

## Мониторинг и управление

### Просмотр логов

Для просмотра логов контейнеров:
```bash
# Для бэкенда
docker-compose logs -f backend

# Для фронтенда
docker-compose logs -f frontend

# Для MongoDB
docker-compose logs -f mongo
```

### Перезапуск контейнеров

Для перезапуска одного контейнера:
```bash
docker-compose restart backend
```

### Проверка состояния

Для проверки состояния API:
```bash
curl http://109.73.192.193:3001/api/health
```

## Лицензия

Этот проект лицензирован под MIT License - см. файл LICENSE для подробностей.
